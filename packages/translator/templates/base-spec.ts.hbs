import { test, expect } from '../../fixtures/wallet.fixture'

// ============================================================================
// Test: {{testName}}
// ============================================================================

test('{{testName}}', async ({ wallet, page }) => {
{{#each steps}}
  // ══════════════════════════════════════════════════════════════════════════
  // STEP {{stepNumber}}: {{comment}}
  // ══════════════════════════════════════════════════════════════════════════
{{#each codeLines}}
  {{{this}}}
{{/each}}

{{/each}}
})
